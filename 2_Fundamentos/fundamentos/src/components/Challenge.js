const Challenge = () => {
  const a = 20;
  const b = 20;
  return (
    <div>
      <h1>Soma de valores</h1>
      <button onClick={() => console.log(a + b)}>Clique aqui</button>
    </div>
  );
};

export default Challenge;
